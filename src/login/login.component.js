'use strict';

angular.module('login', [])
    .component('login', {
        templateUrl: 'login/login.html',
        controller: function ($scope, $rootScope, $window) {
           
        }
    });